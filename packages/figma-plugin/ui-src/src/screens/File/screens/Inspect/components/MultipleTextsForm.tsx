import { Stack, TextField } from 'design-system'
import { Text } from '../../../../../../../shared-types'

interface SingleTextFormProps {
  texts: Text[]
}

export const MultipleTextsForm = ({ texts }: SingleTextFormProps) => {
  return <Stack direction="column">Multiple</Stack>
}
