{
  "name": "recontentapp",
  "license": "AGPL-3.0-only",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "pre-commit": "lint-staged",
    "lockfile:lint": "npx yarn-deduplicate --fail --list -s highest yarn.lock",
    "lockfile:fix": "npx yarn-deduplicate -s highest yarn.lock",
    "prepare": "husky",
    "prettier:lint": "prettier -c .",
    "prettier:fix": "prettier -w .",
    "file-formats:build": "yarn workspace file-formats build",
    "email-templates:build": "yarn workspace email-templates build",
    "openapi:generate": "openapi-kit generate -f ./openapi/private-api.yml -o ./packages/api/src/generated --types && openapi-kit generate -f ./openapi/public-api.yml -o ./packages/api/src/generated/public --types && openapi-kit generate -f ./openapi/private-api.yml -o ./packages/app/src/generated && openapi-kit generate -f ./openapi/public-api.yml -o ./packages/cli/src/generated --types --apiClient",
    "openapi:format": "openapi-format ./openapi/private-api.yml -o ./openapi/private-api.yml && openapi-format ./openapi/public-api.yml -o ./openapi/public-api.yml",
    "setup": "yarn openapi:generate && yarn file-formats:build && yarn email-templates:build",
    "start:app": "yarn workspace app start",
    "start:api": "yarn workspace api start",
    "localstack:start": "yarn localstack:s3",
    "localstack:s3": "aws --profile recontent --endpoint-url=http://localhost:4566 s3api create-bucket --bucket recontent-public --region us-east-1 && aws --profile recontent --endpoint-url=http://localhost:4566 s3api put-bucket-policy --bucket recontent-public --policy file://localstack/s3-bucket-policy.json",
    "stripe:listen": "stripe listen --forward-to localhost:3000/webhooks/stripe --events customer.subscription.created,customer.subscription.trial_will_end,customer.subscription.updated,customer.subscription.deleted"
  },
  "devDependencies": {
    "eslint": "8.57.0",
    "husky": "9.0.11",
    "lint-staged": "15.2.2",
    "openapi-format": "1.16.1",
    "openapi-kit": "0.6.1",
    "prettier": "3.2.5"
  }
}
